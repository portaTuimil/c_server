# Installation
Execute the command `make final` to compile the code then:
```
$ ./c_server  -s (SERVER_MODE) -a (ipadress)
```
in order to run a a server or:
```
$ ./c_server  -c (SERVER_MODE) -a (ipadress)
```
in order to connect.
